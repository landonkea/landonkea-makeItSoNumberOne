# ── requirements.txt — all Python libraries for development ──────
# End users DON'T install these — they run the standalone .exe/.app.
# Install with: pip install -r requirements.txt

# ── Wake word detection (used in ALL modes) ─────────────────────
# Detects "Computer" on the microphone. Tiny, fast, works offline.
pvporcupine>=3.0.0

# ── Microphone access (used in ALL modes) ───────────────────────
# Reads audio from the microphone on all platforms.
pyaudio>=0.2.13

# ── AI API calls (used in ONLINE mode) ─────────────────────────
# Sends HTTP requests to OpenAI (Whisper) and Anthropic (Claude).
requests>=2.31.0

# ── Configuration (used in ALL modes) ──────────────────────────
# Reads config.yaml (YAML files are easy to read and edit).
pyyaml>=6.0

# ── Offline speech-to-text (used in OFFLINE mode) ──────────────
# Runs speech recognition locally. Download a model from:
# https://alphacephei.com/vosk/models
# Extract to: desktop/models/vosk-model-small-en-us-0.15/
vosk>=0.3.44

# ── System control (optional, for mouse/keyboard actions) ─────
# Uncomment if you want Claude to click, type, and control your PC.
# pyautogui>=0.9.54

# ── Journal entry plugin (optional) ────────────────────────────
# Uncomment if you activate desktop/plugins/journal_entry_plugin.py
# (publishes to landonkea-soliloquy over MQTT).
# paho-mqtt>=2.0.0,<3

# ── Build tool (run when you're ready to make a .exe/.app) ─────
# pyinstaller>=6.0
